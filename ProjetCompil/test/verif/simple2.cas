program
   T : array [1 .. 20] of integer ;      -- Tableau d'entiers
   i : 1 .. 20 ;                         -- Indice de tableau
   a: integer;
begin
   for i := 1 to 20 do 
      T[i] := 2 * i ; 
   end ; 
   a := T[4];
   write(a);
   new_line;
end.

